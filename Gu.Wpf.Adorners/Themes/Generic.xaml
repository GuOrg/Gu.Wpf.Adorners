<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Gu.Wpf.Adorners">
    <Style TargetType="{x:Type local:WatermarkAdorner}">
        <Setter Property="IsHitTestVisible" Value="False" />
        <Setter Property="Opacity" Value="0.5" />
        <Setter Property="Focusable" Value="False"></Setter>
        <!--<Setter Property="TextStyle">
            <Setter.Value>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property=""></Setter>
                </Style>
            </Setter.Value>
        </Setter>-->
        <!--<Setter Property="Visibility">
            <Setter.Value>
                <MultiBinding Converter="{x:Static local:WatermarkVisibilityConverter.Default}">
                    <Binding Path="TextBox"
                             RelativeSource="{RelativeSource Self}" />
                    <Binding Path="TextBox.IsVisible"
                             RelativeSource="{RelativeSource Self}" />
                    <Binding Path="TextBox.Text"
                             RelativeSource="{RelativeSource Self}" />
                    <Binding Path="TextBox.IsKeyboardFocused"
                             RelativeSource="{RelativeSource Self}" />
                </MultiBinding>
            </Setter.Value>
        </Setter>
        <Setter Property="Margin" Value="{Binding AdornedElement.Padding, RelativeSource={RelativeSource Self}}" />-->
    </Style>
</ResourceDictionary>
