pool:
  vmImage: 'windows-2022'

steps:

- task: VSBuild@1
  displayName: build
  inputs:
    configuration: 'Release'
    msbuildArgs: '/restore'

- task: VSTest@2
  displayName: test
  inputs:
    configuration: 'Release'